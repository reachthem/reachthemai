'use client';

interface ServiceSelectionSectionProps {
  advisorPrice: string;
  removalPrice: string;
}

export default function ServiceSelectionSection({ advisorPrice, removalPrice }: ServiceSelectionSectionProps) {
  return <section></section>;
}
